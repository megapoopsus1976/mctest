import { FsfilecontentPipe } from './fsfilecontent.pipe';

describe('FsfilecontentPipe', () => {
  it('create an instance', () => {
    const pipe = new FsfilecontentPipe();
    expect(pipe).toBeTruthy();
  });
});
